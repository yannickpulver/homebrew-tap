# homebrew-tap

Homebrew tap for Yannick Pulver's apps.

## Install

```sh
brew tap yannickpulver/tap
```

## Apps

```sh
brew install --cask raw-viewer
brew install --cask copier
brew install --cask slides
```

## Update

```sh
brew update
brew upgrade --cask raw-viewer copier slides
```
